# v1.2 #10 — Retire BCA VA + static-account manual transfer (design spec)

**Date:** 2026-06-19
**Status:** Design approved (brainstorm gate) — feeds `/spec-plan-pipeline` planning.
**Roadmap source:** `docs/superpowers/specs/2026-06-18-pos-backlog-roadmap-design.md` §#10.
**ADR impact:** amends [ADR-036](../../ADR/036-xendit-dedicated-apis-inline.md) (method set + manual-confirm tier).
**Deps:** #12 inline-messaging (shipped — reuse `FieldMessage`). Couples forward to #6 (clock-out reads the reconciliation query #10 ships).

---

## 1. Problem

Two coupled defects on the payment path:

1. **BCA VA error storm.** `createBcaVaCharge` (`convex/payments/xendit.ts:96`) is LIVE-UNVERIFIED and throws `XENDIT_VA_FAILED` → `BANK_NOT_ACTIVATED_ERROR`. The charge screen's auto-create effect (`src/routes/sale/charge.tsx:173-233`) re-fires `requestPayment({method:"BCA_VA"})` on every tab/invoice change; each failure raises a `toast.error` (`charge.tsx:222`) → a storm of error toasts. BCA VA never completes a sale.
2. **No working non-QRIS tender.** Booth staff need a fallback when a customer can't pay via QRIS. The real-world fallback already in use is a **manual bank transfer** to the company BCA account, with proof posted to the Block M staff WhatsApp group — but the POS has no way to record that sale, so those transactions are invisible to the ledger, the receipt, and reconciliation.

## 2. Goal

- Kill the storm **by construction**: QRIS becomes the sole Xendit method; the BCA VA tab disappears, so the only auto-created invoice is QRIS (live-proven).
- Add a **manual bank-transfer tender**: staff see the static company account, tap a self-attestation confirm, and the sale commits — marked `manual_bca` so it is fully tracked.
- Make the attestation **safe without a photo gate**: a daily + clock-out **itemized reconciliation summary** lets operators validate every manual-BCA deposit against the real BCA account they already control. The per-sale Telegram ticker flags manual-BCA sales distinctly.

## 3. Non-goals (explicit out of scope)

- **Dynamic BCA VA revival** — backlogged (BCA-only, non-universal). Keep the code dormant.
- **In-app photo capture / `_storage` upload / `navigator.share` / `.webp`** — the proof flow stays fully out-of-band in WhatsApp; the POS records only the attestation tap. (Photo work is #3.)
- **The #6 clock-out screen** — #10 ships only the backing reconciliation **query** + the EOD cron hook. #6 builds the clock-out UI and its own staff-gated read wrapper.
- **Real Xendit refunds** — #9, spike-gated, lands after #10.
- **Removing the `BCA_VA` schema literal** — deploy-skew-fatal; breaks historical `pos_xendit_invoices` rows. Hide UI only.

## 4. Locked decisions (from brainstorm; do not relitigate)

| # | Decision |
|---|----------|
| L1 | Hide the BCA VA tab in `charge.tsx`. **Keep** `createBcaVaCharge`, the FVA webhook parser, and the `BCA_VA` schema literal. QRIS becomes the sole Xendit method. |
| L2 | Third pay option **"Bank transfer (manual)"** showing the static account, sourced from new editable `pos_settings` fields (manager-session CRUD). |
| L3 | **Staff self-confirm — NO manager-PIN, NO in-app photo.** Staff tap *"I confirm payment proof has been sent to the Block M staff WhatsApp group"* → txn commits. |
| L4 | New `confirmed_via: "manual_bca"` marker (distinct from manager-override `manual`), committed via a **new staff-session mutation** reusing `_confirmPaid_internal`. |
| L5 | Reconciliation is the compensating control: **itemized** manual-BCA summary (each txn's time, amount, staff + count + total IDR) at EOD (founders cron) and clock-out (#6). #10 ships the query + EOD hook. |
| L6 | Per-sale `txn_ticker` message flags `manual_bca` sales distinctly ("check the BCA account for the deposit"). |
| L7 | Light resilience: auto-create catch → inline `FieldMessage` (#12), guard re-fire loop. |
| L8 | **DEVIATION (ADR-036):** staff self-confirm replaces the manager-PIN gate for manual BCA; reconciliation is the compensating control. Document in the ADR-036 amendment. |
| L9 | **Add a `manual_bca_enabled` toggle** (manager-editable) so the manual tender can be hidden without a code change. *(Lucas, brainstorm gate.)* |

## 5. Resolved open decisions

| Decision | Resolution | Rationale |
|---|---|---|
| `pos_settings` field names | `manual_bca_enabled`, `manual_bca_bank_name`, `manual_bca_account_name`, `manual_bca_account_number` (all optional, read-time defaults). | Mirrors `receipt_*` / `txn_ticker_enabled` pattern exactly. |
| Confirm fn type | **`mutation`** (not action), in `convex/payments/public.ts`. | No PIN, no Xendit → no node runtime; a mutation is atomic and avoids a mutation↔action deploy-skew hazard. `manuallyConfirmPayment` is an action only because it runs argon2. |
| Marker mechanism | Add `"manual_bca"` literal to the `_confirmPaid_internal` `source` union and the `pos_transactions.confirmed_via` union. | The funnel already stamps `confirmed_via = source` and the ticker already branches on it — one additive literal threads the marker through funnel, ticker, and reconciliation with **no new column**. |
| Audit verb | Reuse `payment.confirmed` with `metadata.source = "manual_bca"`. | Verb-reuse over mint (see prior triple-review lessons). Reconciliation queries `confirmed_via`, not the audit log. |
| Attesting-staff attribution | No new txn column — the attesting staff is the cashier (`txn.staff_id`). Thread `confirm_staff_id` only for the audit `actor_id`. | Booth manual-BCA confirm is done by the logged-in cashier; `staff_id` already records them. |
| Enabled toggle surface | Fold `enabled` into `updateManualBcaConfig` (one manager mutation: enabled + 3 account fields). | The flag + account fields are one logical config block edited on the same screen. A standalone kill-switch can be added later if needed. |
| Reconciliation query home | Internal-only `_manualBcaReconciliation_internal` in `convex/transactions/internal.ts`; EOD founders cron consumes it. | #6 owns the clock-out UI and adds its public/staff-gated wrapper; #10 ships internal + EOD only (YAGNI). |
| EOD delivery | Extend the **existing** founders cron (22:00 WIB) payload + `renderFoundersSummary`, not a sibling cron. | One cron, one message; additive. |

## 6. Architecture

### 6.1 Settings (`convex/settings/`)

**Schema** (`settings/schema.ts`) — add to `pos_settings` (all optional, additive; no migration):

```ts
manual_bca_enabled: v.optional(v.boolean()),
manual_bca_bank_name: v.optional(v.string()),
manual_bca_account_name: v.optional(v.string()),
manual_bca_account_number: v.optional(v.string()),
```

**Defaults** (`settings/internal._getSettings_internal`) — add a `manual_bca` sub-object mirroring the existing `receipt` sub-object, single-sourced via a `MANUAL_BCA_DEFAULTS` const:

```
enabled: true                      // ships on (symmetric with txn_ticker_enabled default true)
bank_name: "BCA"
account_name: "PT Malo Group Bahagia"
account_number: "6044830994"
```

**Manager CRUD** (`settings/public.ts`, mirror `getReceiptConfig` / `updateReceiptConfig`):
- `getManualBcaConfig({ sessionId })` → `requireManagerSession`; returns `{ enabled, bank_name, account_name, account_number }`.
- `updateManualBcaConfig({ idempotencyKey, sessionId, enabled, bank_name, account_name, account_number })` → withIdempotency + `authCheck: requireManagerSession`; 120-char bound on each string (reuse the receipt-config bound); audit `payment.confirmed`-style → emit `settings.manual_bca_updated` (new audit string — `audit_log.action` is `v.string()`, no enum migration). Patch the singleton (insert with `founders_summary_enabled: true` when absent, like `updateReceiptConfig`).

**Staff read** (`settings/public.ts`):
- `getManualBcaAccount({ sessionId })` → `requireSession` (any active staff); returns `{ enabled, bank_name, account_name, account_number }`. The charge screen uses `enabled` to show/hide the manual tab and renders the three fields.

> **Account-number validation:** `account_number` is digits only in practice but stored as a string (leading-zero safe). Bound at 120 chars; no numeric coercion (ADR-015 is about money, not account ids).

### 6.2 Confirm path (`convex/payments/` + `convex/transactions/internal.ts`)

**`_confirmPaid_internal`** (`transactions/internal.ts:142`) — THE FUNNEL. Changes:
- `source` arg union: add `v.literal("manual_bca")`.
- New optional arg `confirm_staff_id: v.optional(v.id("staff"))` — the attesting cashier (audit actor for the manual-BCA path).
- Confirmed-via patch (step 7): `confirmed_via: args.source` already stores the source verbatim → `"manual_bca"` flows through once the schema union accepts it.
- Audit (step 8) + the `confirmed_on_terminal` branch: for `source === "manual_bca"`, set `actor_id: args.confirm_staff_id ?? "system"` and `source: "booth_inline"`. Keep `action: "payment.confirmed"`, `metadata.source: "manual_bca"`. (Webhook/polling keep `source:"system"`; manager `manual` keeps its `approvalSource` logic.)
- Ticker scheduling (step 9) unchanged — fires once per paid txn for all sources.

**`pos_transactions.confirmed_via`** (`transactions/schema.ts:38`) — add `v.literal("manual_bca")` to the union.

**`_getTxnForTicker_internal`** (`transactions/internal.ts:752`) — widen the returned `confirmed_via` union type to include `"manual_bca"`.

**New mutation `confirmManualBcaPayment`** (`convex/payments/public.ts`):
```ts
args: { idempotencyKey: v.string(), sessionId: v.id("staff_sessions"), txnId: v.id("pos_transactions") }
returns: { confirmed: true, receiptNumber: string }
```
- withIdempotency-wrapped (cache key `args.idempotencyKey`); `authCheck: requireSession` (ADR-013 — gate the cache lookup itself so a replay can't read back `{confirmed}` without a session).
- Handler: resolve `{ staffId } = requireSession`; `runMutation(_confirmPaid_internal, { txnId, source:"manual_bca", confirm_staff_id: staffId })`; read back `txn`, assert `status === "paid" && receipt_number` else throw `RECEIPT_UNCONFIRMED` (mirrors `_onPaidManual_internal`'s C4 guard); return `{ confirmed: true, receiptNumber }`.
- **No Xendit call, no manager PIN.** The funnel's status-guard makes this idempotent and safe against a webhook/cancel race (a non-`awaiting_payment` txn no-ops). Reuses snapshot pricing + the `by_line_and_sku` double-decrement guard (business rule #17) because it routes through the same funnel.

> **Why a new mutation, not `_onPaidManual_internal`:** that internal mutation mandates `mgr_approver_id: v.id("staff")` (non-optional) and stamps `confirmed_mgr_approver_id`. Manual-BCA has no manager approver, so it funnels to `_confirmPaid_internal` directly via this new public mutation rather than threading a fake manager through the manager path.

### 6.3 Charge screen (`src/routes/sale/charge.tsx`, `src/hooks/useXenditPayment.ts`)

- `Method` type → `"QRIS" | "MANUAL_BCA"` (drop `"BCA_VA"` from the FE union; the backend `requestPayment` union keeps `BCA_VA` valid-but-unused).
- Tabs: `QRIS` | `Bank transfer (manual)`. Render the manual tab only when `getManualBcaAccount().enabled`.
- **QRIS tab:** unchanged (existing auto-create + QR render).
- **MANUAL_BCA tab:** does **not** create a Xendit invoice. Renders the static account (bank / name / number, copy-to-clipboard on the number) + a required-checkbox attestation *"I confirm payment proof has been sent to the Block M staff WhatsApp group"* + a confirm button (disabled until checked). On confirm → `confirmManualBcaPayment({ sessionId, txnId, idempotencyKey })`; on success navigate to `/sale/charge/:txnId/success` (same as manager override). Inline `FieldMessage` (error tone) on failure — **no toast**.
- The auto-create effect (`charge.tsx:173-233`) must **skip** when `selectedMethod === "MANUAL_BCA"` (no invoice for the manual tab). Its catch branch (`charge.tsx:216-223`) converts `toast.error(msg)` → an inline `FieldMessage` (L7) and keeps the re-fire guard (`requestedMethods`).
- `useXenditPayment` still drives the QRIS subscription; the manual tab doesn't subscribe to an invoice (there is none). `phase.kind === "paid"` still navigates to success when the confirm commits (the reactive `txn.status` flips to `paid`).

### 6.4 Ticker (`convex/telegram/txnTicker.ts`, `convex/lib/telegramHtml.ts`, `convex/telegram/send.ts`)

- `sendTxnTicker` (`txnTicker.ts:73`): compute `isManualBca = txn.confirmed_via === "manual_bca"`. Label via `instrumentLabel(..., { isManual, isManualBca })` → `"Manual BCA"` (distinct from manager-override `"Manual"`).
- `TxnTickerPayload` (`telegramHtml.ts:299`) + the `txn_ticker` payload validator (`send.ts:122-126`): add `manual_bca: boolean` (or `v.optional(v.boolean())` for back-compat with in-flight queued sends).
- `renderTxnTicker` (`telegramHtml.ts:309`): when `manual_bca`, append a visually distinct alert line: `⚠️ <b>MANUAL</b> — check the BCA account for the deposit`.

### 6.5 Reconciliation + EOD (`convex/transactions/internal.ts`, `convex/telegram/foundersSummary.ts`, `convex/lib/telegramHtml.ts`, `convex/telegram/send.ts`)

**New internal query `_manualBcaReconciliation_internal`** (`transactions/internal.ts`):
```ts
args: { dayStartMs: v.number(), dayEndMs: v.number() }
returns: { items: Array<{ paidAt: number; total: number; staffName: string; receiptNumber: string }>; count: number; totalIdr: number }
```
- Query `by_status_paid_at` paid rows in `[dayStartMs, dayEndMs)`, filter `confirmed_via === "manual_bca"` in JS, sort ascending by `paid_at` (chronological for reconciliation reading).
- Resolve staff names via `_listStaffNames_internal` → Map (no N+1).
- Returns `count` + `totalIdr` (sum of `total`) for the tally line.

**EOD founders summary** (`foundersSummary.sendFoundersSummary`): after the existing `_dailySalesSummary_internal` call, also call `_manualBcaReconciliation_internal` for the same WIB window and thread `manualBca` into the `shift_summary` payload.

- `FoundersSummaryPayload` (`telegramHtml.ts:90`) + the `shift_summary` validator (`send.ts:64-68`): add `manualBca: { count, totalIdr, items: [...] }`.
- `renderFoundersSummary` (`telegramHtml.ts:97`): append a manual-BCA section — header line `🏦 Manual BCA: {count} txn · Rp {totalIdr}` followed by itemized lines `• {wibTime} · Rp {amount} · {staffName} ({receipt})`. **Cap** the itemized lines (e.g. first 30) with an explicit `…+N more — see POS` overflow note so the message stays under Telegram's 4096-char limit; **never silently truncate** the count/total (those are computed over all rows). When `count === 0`, omit the section entirely.

## 7. Business rules honored

- **ADR-013 idempotency:** `confirmManualBcaPayment` takes `idempotencyKey` + `withIdempotency` + `authCheck` (ESLint-enforced public-mutation contract).
- **ADR-031 server time:** `paid_at` is set inside `_confirmPaid_internal` via `Date.now()` — unchanged.
- **ADR-007 audit:** confirm emits `payment.confirmed` (`metadata.source:"manual_bca"`); settings emit `settings.manual_bca_updated`.
- **ADR-015 money:** all amounts integer rupiah; `account_number` is an identifier string, not money.
- **Rule #17 snapshot + double-decrement:** the confirm routes through `_confirmPaid_internal`, so snapshot pricing + the `by_line_and_sku` movement-dedup guard hold.
- **Deploy-skew:** the new tender is a **mutation** (no mutation↔action rename); the `BCA_VA` literal and the `requestPayment` method union are untouched. Backend + frontend ship in one PR/build (atomic), so the FE never calls a renamed surface.
- **SCHEMA.md first:** new `pos_settings` fields documented (`docs/SCHEMA.md` §`pos_settings`, line ~548) before/with the schema change; new audit string added to the audit-actions list (~line 765).
- **Manager-session vs staff-session tiering:** settings CRUD (incl. the enabled toggle) = manager-session (low-stakes config, like receipt-config); the sale confirm = staff-session (locked tender).

## 8. Files touched

| File | Change |
|---|---|
| `convex/settings/schema.ts` | +4 optional `manual_bca_*` fields |
| `convex/settings/internal.ts` | `MANUAL_BCA_DEFAULTS` + `manual_bca` sub-object in `_getSettings_internal` |
| `convex/settings/public.ts` | `getManualBcaConfig`, `updateManualBcaConfig`, `getManualBcaAccount` |
| `convex/payments/public.ts` | `confirmManualBcaPayment` mutation |
| `convex/transactions/schema.ts` | `confirmed_via` union += `"manual_bca"` |
| `convex/transactions/internal.ts` | `_confirmPaid_internal` (source union, `confirm_staff_id`, audit branch); `_getTxnForTicker_internal` type; new `_manualBcaReconciliation_internal` |
| `convex/telegram/txnTicker.ts` | manual-BCA label + payload flag |
| `convex/lib/telegramHtml.ts` | `renderTxnTicker` alert line; `renderFoundersSummary` manual-BCA section; payload types |
| `convex/telegram/send.ts` | `txn_ticker` + `shift_summary` payload validators |
| `convex/telegram/foundersSummary.ts` | call `_manualBcaReconciliation_internal`; thread into payload |
| `src/routes/sale/charge.tsx` | Method type, tabs, manual tender UI, inline FieldMessage |
| `src/hooks/useXenditPayment.ts` | (verify) manual tab needs no invoice subscription |
| `docs/SCHEMA.md` | document `manual_bca_*` fields + `settings.manual_bca_updated` audit string |
| `docs/ADR/036-xendit-dedicated-apis-inline.md` | amendment (method set + manual-confirm tier deviation) |
| tests | payment path → **tests REQUIRED** (CLAUDE.md #7): confirm mutation (happy + idempotent re-fire + non-awaiting no-op + RECEIPT_UNCONFIRMED), reconciliation query, ticker manual flag, settings CRUD, charge-screen render (manual tab shows/hides on `enabled`) |

## 9. Test plan (payment path — required)

- **`confirmManualBcaPayment`**: happy path commits + allocates receipt + marks `confirmed_via:"manual_bca"`; idempotent re-fire returns cached result (no double receipt); non-`awaiting_payment` txn no-ops without false success (RECEIPT_UNCONFIRMED); no-session rejects before cache read.
- **`_confirmPaid_internal` manual_bca branch**: audit row `actor_id = confirm_staff_id`, `source = booth_inline`, `metadata.source = "manual_bca"`; stock decrement + voucher redemption still run.
- **`_manualBcaReconciliation_internal`**: counts/totals only `manual_bca` rows in window; excludes QRIS/manual; correct staff-name join; ascending order.
- **Ticker**: `manual_bca` txn renders the MANUAL alert line + "Manual BCA" label; QRIS unchanged.
- **Founders summary**: section present with itemized lines when manual-BCA sales exist; omitted at `count===0`; overflow cap note when > cap.
- **Settings**: `updateManualBcaConfig` round-trips incl. `enabled`; `getManualBcaAccount` returns defaults when row absent; manager gate on CRUD, staff gate on the account read.
- **Charge screen** (component/E2E): manual tab hidden when `enabled:false`; confirm disabled until attestation checked; success navigates; failure shows inline FieldMessage not toast; QRIS tab no longer storms (BCA VA tab gone).

## 10. Rollout / verification

- Backend + frontend ship in one squash PR (atomic build). No live-Xendit gate (manual BCA never calls Xendit; QRIS is already live-proven).
- Post-deploy: set the prod `pos_settings` manual-BCA fields via `/mgr` (or accept code defaults), confirm the manual tab renders the account, run one manual-BCA test sale → verify it appears in the next founders summary + flags MANUAL in the ticker.
- The execution session closes out with the standard QA gate: `/triple-review` → `/simplify xhigh`.

## 11. Self-review

- **Placeholders:** none — all field names, signatures, and line refs grounded against current `main` code.
- **Consistency:** the `manual_bca` marker is single-sourced through `confirmed_via`; ticker, reconciliation, and (future) #6 all read the same field. No second marker column.
- **Scope:** one slice — close BCA VA + add manual tender + reconciliation/EOD. #6 UI, #9 refunds, #3 photos explicitly deferred.
- **Ambiguity resolved:** confirm = mutation; enabled toggle folded into config mutation; reconciliation internal-only + EOD; audit verb reused; attesting staff = cashier.
