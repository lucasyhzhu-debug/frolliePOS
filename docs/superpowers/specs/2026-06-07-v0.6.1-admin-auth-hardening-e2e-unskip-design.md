# v0.6.1 — Admin-action auth hardening + e2e un-skip (design spec)

**Date:** 2026-06-07
**Status:** Draft (pre-staffreview)
**Origin:** Retrospective code review of the v0.6 merge (PR #56). Two follow-up tasks
on `docs/PROGRESS.md` (`v06-fu-actioncache-authcheck`, `v06-fu-e2e-unskip`) plus
closing out two already-landed fixes (`v06-fu-voucher-shared-helper`,
`v06-fu-spoilage-comment-fix`).

> **One combined phase, two waves.** Wave A is a well-understood backend
> architecture change (directly plannable). Wave B is **evidence-gated**: it does
> NOT pre-commit to a fix — it root-causes the e2e failures first, because the
> existing #43 hypothesis mirrors the *refuted* #44 misdiagnosis.

---

## 1. Motivation

### 1a. Wave A — the action-cache leaks cached results to unauthenticated callers

`convex/idempotency/action.ts::withActionCache` is the action-layer idempotency
helper (v0.5.3b extraction). Its current shape:

```ts
export async function withActionCache<T>(
  ctx: ActionCtx,
  params: { key: string; mutationName: string },
  fn: () => Promise<T>,
): Promise<T> {
  const cached = await ctx.runQuery(internal.idempotency.internal._lookup_internal, { key: params.key });
  if (cached) return JSON.parse(cached) as T;          // ← returns BEFORE any auth
  const result = await fn();
  await ctx.runMutation(internal.idempotency.internal._writeCache_internal, { ... });
  return result;
}
```

Every PIN-gated admin action delegates to it and performs auth (`verifyManagerPinOrThrow`)
**inside `fn()`** — i.e. *after* the cache lookup. Consequence: a caller who knows a
previously-used `idempotencyKey` can call the action with that key (and any garbage
session/PIN) and receive the cached return value — an opaque document id
(`Id<"pos_vouchers">`, `{ event_id }`, `{ ok: true }`) — with **no valid credentials**.

This violates the principle codified for **mutations** in CLAUDE.md rule #20 /
[ADR-013](../../ADR/013-idempotency-keys.md) and
[`docs/PATTERNS/idempotency-dual-call-authcheck.md`](../../PATTERNS/idempotency-dual-call-authcheck.md):
*authCheck must run BEFORE the cache lookup*. The mutation HOF `withIdempotency`
already implements this — it takes an `authCheck?` option that runs first
(`convex/idempotency/internal.ts:57,61`). The **action** helper simply never grew
the equivalent.

**Scope of the gap (7 call sites, all PIN-gated admin actions):**

| Module | Action | Cached return |
|---|---|---|
| `staff/actions.ts` | `setStaffRole` | `{ ok: true }` |
| `staff/actions.ts` | `deactivateStaff` | `{ ok: true }` |
| `catalog/actions.ts` | `createProduct` | `Id<"pos_products">` |
| `catalog/actions.ts` | `createInventorySku` | `Id<"pos_inventory_skus">` |
| `catalog/actions.ts` | `updateProductPricing` | `{ ok: true }` |
| `vouchers/actions.ts` | `createVoucher` | `Id<"pos_vouchers">` |
| `inventory/actions.ts` | `recordSpoilage` | `{ event_id, ... }` |

This is **systemic** (a v0.5.3b pattern), **not a v0.6 regression** — v0.6's two
actions faithfully followed prior art. Real-world severity is low (the
`idempotencyKey` is an unguessable client UUID and the payload is an opaque id),
but it is a genuine divergence from a documented invariant and should be closed
once, at the helper, so it cannot recur.

### 1b. Wave B — 6 Playwright specs are quarantined for THREE distinct, evidenced reasons

The v0.6 merge shipped a Playwright scaffold, but 6 of 7 functional specs are
`test.skip`'d (only `auth` sign-in runs):
`sale-qris`, `sale-bca-va`, `voucher-online`, `voucher-offline`, `refund`, `spoilage`.

Issue #43's title frames this as *"hard-nav session loss"* (`useSession.isDead`
on transient-null). **Reading the actual per-spec skip headers refutes that.**
Each spec carries a skip-comment-template block (observed failure mode + PR #52
Playwright run id + `.claude/pw-report/...` evidence path), and they document
**three unrelated causes** — the session/nav path actually *works* in every case.
The #43 single-cause framing is itself a mis-attribution (a meta-instance of the
[#44 misdiagnosis](../../postmortems/2026-06-issue-44-misdiagnosis.md) pattern —
the very reason this spec is evidence-gated).

| Cluster | Specs | Documented cause (with evidence) |
|---|---|---|
| **C1 — Xendit test-mode simulate 404** | `sale-qris`, `sale-bca-va`, `voucher-online`, `refund` (transitive) | The id we publish (`xendit_invoice_id` for QR / `external_id` for FVA) ≠ what the simulate endpoints expect. `simulateQrisPaid` → `404 DATA_NOT_FOUND` (`e2e/helpers/xendit-simulate.ts:25`, run `27054044763`); `simulateBcaVaPaid` → `404 CALLBACK_VIRTUAL_ACCOUNT_NOT_FOUND_ERROR` (`:37`, run `27055135440`). a11y/nav/QR-render all confirmed working — each spec *reaches* the simulate step. `refund`/`voucher-online` are blocked only because they call `simulateQrisPaid` upstream. |
| **C2 — seed exposes no stable test IDs** | `voucher-offline` | `seed/actions:reset` returns `{wiped, inserted}` only; the concurrent offline-apply-vs-archive race needs the manager `sessionId` + created `voucherId`. Spec has `<TBD>` execSync tokens; un-skipping as-is false-greens via the existing try/catch. |
| **C3 — spoilage submit stays disabled** | `spoilage` | After `.fill()` of Qty+Reason, "Log spoilage now" stays `[disabled]` (Gate-1 page snapshot, run `27054044763`). Likely the `replace(/[^\d]/g, "")` onChange at `src/routes/mgr/spoilage.tsx:270` not firing for Playwright `fill`, or an extra disable condition. Needs a headed local repro. |

**The lockout stub** (`auth.spec.ts:24`, empty `test.skip`) is a 4th, separate
item (Sonner toast race, needs a `[data-locked]` readiness signal) — **out of
scope** (§N4).

**Therefore Wave B is per-cluster, evidence-gated:** each cluster has a short
verify step against its already-captured evidence, then a targeted fix, then an
incremental un-skip. Because the evidence already exists, the fixes are concrete
(below) — but each cluster's verify step must *confirm the documented cause still
holds on current `main`* before the fix lands (the documented runs predate v0.6.1).

---

## 2. Goals / Non-goals

**Goals**
- G1: Close the action-cache auth-before-lookup gap at the helper, with all 7
  call sites wired and a regression test per behaviour.
- G2: Make it structurally impossible to add a cached admin action without auth
  (required param, not optional).
- G3: Document the decision as ADR-046, extending rule #20 to the action layer.
- G4: Verify the three documented per-cluster causes (C1/C2/C3) still hold on
  current `main` before fixing each — no fix on a stale or assumed cause.
- G5: Un-skip the 6 quarantined specs once they pass, one at a time, per cluster.
- G6: Close out the 2 already-landed follow-ups on the board.

**Non-goals**
- N1: No change to the mutation-side `withIdempotency` (already correct).
- N2: No change to which actions are PIN-gated vs session-gated (rule #22 tiering
  is unchanged).
- N3: No predetermined e2e fix — the fix is whatever B1's evidence dictates.
- N4: Writing the empty lockout-test stub (`auth.spec.ts:24`) is out of scope
  (it's an unwritten stub, not a quarantined spec); note as a future follow-up.
- N5: No broadening of e2e coverage beyond un-skipping the existing 6 specs.

---

## 3. Wave A — Action-cache auth-before-lookup

### 3.1 Helper change

`withActionCache` gains a **required** `authCheck` parameter that runs before the
cache lookup, mirroring `withIdempotency`'s `authCheck` option:

```ts
export async function withActionCache<T>(
  ctx: ActionCtx,
  params: { key: string; mutationName: string },
  authCheck: () => Promise<void>,   // NEW — runs BEFORE _lookup_internal; throws on bad auth
  fn: () => Promise<T>,
): Promise<T> {
  await authCheck();                                    // fail-cheap gate, pre-cache
  const cached = await ctx.runQuery(internal.idempotency.internal._lookup_internal, { key: params.key });
  if (cached) return JSON.parse(cached) as T;
  const result = await fn();
  await ctx.runMutation(internal.idempotency.internal._writeCache_internal, { ... });
  return result;
}
```

`authCheck` is a zero-arg closure (symmetric with `fn`); each caller captures
`ctx` + `args` and asserts a live manager session. The expensive argon2 PIN
verify (`verifyManagerPinOrThrow`) **stays inside `fn()`** — so the existing
"cache hit skips PIN on a legit retry" optimisation (documented at
`catalog/actions.ts:88`) is preserved. Only the *unauthenticated-replay* hole closes.

**Why required, not optional:** every current `withActionCache` caller is a
PIN-gated admin action that needs auth. `loginWithPin`/`changePin` deliberately
do *not* use the helper (custom post-cache logic). Making the param required
closes the hole *by construction* — you cannot add a cached admin action that
skips auth — which is stronger than an optional param + a lint rule to remember.

### 3.2 Shared action-side manager assert

To avoid 7 copies of the manager check, add a small action-context helper
(home: `convex/auth/verifyPin.ts`, already the action-side manager funnel):

```ts
/** Action-context fail-cheap manager-session assert. Throws if the session is
 *  ended/absent or the staff is not an active manager. Does NOT verify PIN. */
export async function assertManagerSessionInAction(
  ctx: ActionCtx,
  sessionId: Id<"staff_sessions">,
): Promise<void> {
  const resolved = await ctx.runQuery(
    internal.auth.internal._resolveSessionRole_internal,  // existing, returns {staffId,deviceId,role}|null
    { sessionId },
  );
  if (!resolved || resolved.role !== "manager") {
    throw new Error("MANAGER_SESSION_REQUIRED");
  }
}
```

This reuses the existing non-throwing `_resolveSessionRole_internal`
(`convex/auth/internal.ts:340`).

**Resolution-parity invariant (staffreview).** `verifyManagerPinOrThrow`
(`convex/auth/verifyPin.ts:68`) currently resolves the session via a *different*
path — `api.auth.public.getSession` + `internal.auth.internal._getStaffPinHash_internal`
for the role/active check — than the `_resolveSessionRole_internal` proposed here.
Two independent "is this a live manager session?" resolvers can silently diverge
(e.g. if one later adds a device-binding or `ended_at` nuance). **The pre-cache
gate MUST accept exactly the set of sessions `verifyManagerPinOrThrow` accepts**,
or a legit call passes the gate then fails inside `fn` (or worse, the gate is
laxer than the inner check). Both currently require *active staff, role ==
"manager", session not ended*, so they agree today. To keep them in lockstep:
- The plan adds a **parity test**: a staff-role session and an ended session are
  each rejected by BOTH `assertManagerSessionInAction` and (independently)
  `verifyManagerPinOrThrow`.
- Stretch (OQ3): refactor `verifyManagerPinOrThrow` to resolve session+role via
  `_resolveSessionRole_internal` too, so there is a single resolver — deferred
  unless the parity test is awkward to express against two paths.

### 3.3 Call-site shape (×7)

```ts
handler: async (ctx, args): Promise<Id<"pos_vouchers">> =>
  withActionCache(
    ctx,
    { key: args.idempotencyKey, mutationName: "vouchers.createVoucher" },
    () => assertManagerSessionInAction(ctx, args.sessionId),   // NEW pre-cache gate
    async () => {
      /* ...existing validation + verifyManagerPinOrThrow + commit, UNCHANGED... */
    },
  ),
```

### 3.4 ADR-046

`docs/ADR/046-action-cache-auth-before-lookup.md` — Accepted. Extends ADR-013 /
rule #20 to the action layer: every action-level idempotency cache must gate a
live-session authCheck before the cache lookup; PIN verify stays inside the
cached body. References the threat (cached-result replay) and the
required-param enforcement. ADR must also note the **consequence**: because
authCheck runs before the lookup, a session that has ended between the original
call and a retry can no longer replay the cached result — identical to the
mutation-side `withIdempotency` behaviour, and acceptable (an expired session
should not read a cached success).

### 3.5 Tests (per action behaviour)

For each of the 7 actions (or a representative + a shared parametrised test):
- **New — cached-replay rejected (the core regression guard):** seed a real cache
  row for key `K` (via a successful manager call), then call again with `K` and an
  *ended/absent* or *staff-role* session. Expect a throw (`MANAGER_SESSION_REQUIRED`)
  and **no cached value returned** — proving authCheck runs before the lookup.
- **New — optimisation preserved:** a legit idempotent retry (same `K`, same valid
  manager session) returns the cached value **without re-running the argon2 PIN
  verify**. Assert `verifyManagerPinOrThrow`/`argon2Verify` is NOT invoked on the
  cache-hit path (spy/call-count), so the "cache hit skips PIN" benefit survives
  the change.
- **New — resolution parity:** a staff-role session and an ended session are each
  rejected by BOTH `assertManagerSessionInAction` and `verifyManagerPinOrThrow`
  (guards the two-resolver drift risk in §3.2).
- **Unchanged-green:** existing happy-path + idempotent-replay tests still pass.

---

## 4. Wave B — e2e un-skip by cluster (evidence-gated)

Each cluster: **verify** the documented cause still holds on current `main` →
**fix** → **un-skip** the cluster's specs one at a time (green before the next
`test.skip` is removed). Order by leverage: C1 (4 specs) → C2 → C3.

### 4.1 C1 — Xendit test-mode simulate id mismatch (un-skips 4 specs)

**Verify:** Before touching the helper, consult [`docs/xendit-reference/`](../../xendit-reference/)
(the source-cited Xendit knowledge base — memory: facts live there, check before
re-researching) for the QR Codes API id vs `reference_id` distinction and the FVA
simulate id. Confirm what `xendit_invoice_id` (QR) and `external_id` (FVA) actually
hold for a freshly-charged dev txn (read `charge.tsx:524-525` / `:549-558` and the
`pos_xendit_invoices` row), and what `POST /qr_codes/{id}/payments/simulate` and the
FVA simulate endpoint expect. Reproduce the 404 once locally to confirm it persists.

**Fix:** correct the id the simulate helper sends (most likely
`e2e/helpers/xendit-simulate.ts` uses the wrong stored field; the alternative is
that `charge.tsx`'s `data-qr-id`/`data-external-id` exposes the wrong field). Whichever
the evidence shows — change the **single** wrong source, not both. Note: QRIS + BCA
FVA were live-verified end-to-end via test-mode simulate during ADR-036 (memory:
`xendit-va-number-unverified`), so the *endpoints* work — this is an id-plumbing bug
in the helper/exposure, not a missing capability.

**Un-skip (in order, green-gated):** `sale-qris` → `sale-bca-va` → `voucher-online`
→ `refund`. `refund`/`voucher-online` should pass once `simulateQrisPaid` works,
since their own selectors are already confirmed unblocked.

### 4.2 C2 — seed exposes stable test IDs (un-skips voucher-offline)

**Verify:** confirm `convex/seed/actions.ts::reset` still returns `{wiped, inserted}`
only, and that `voucher-offline.spec.ts` needs a manager `sessionId` + a created
`voucherId` for the concurrent-archive race.

**Fix:** extend `seed:reset` (or add a small dev-only internal) to expose stable
test IDs — at minimum the seeded manager's session id and a known voucher id — so
the spec's `execSync` steps resolve real values instead of `<TBD>`. Keep it
dev/seed-scoped (prod-slug deny-list already guards `seed:reset`). Replace the
`<TBD>` tokens; ensure a real failure throws loudly (no try/catch false-green).

**Un-skip:** `voucher-offline`.

### 4.3 C3 — spoilage submit-disable (un-skips spoilage)

**Verify:** headed local Playwright repro. Confirm whether the Qty/Reason `.fill()`
fires React `onChange` (inspect `src/routes/mgr/spoilage.tsx:270`
`replace(/[^\d]/g, "")`) and whether the submit-disable predicate matches what the
spec satisfies (cross-check the disabled-gate vs submit-validator — memory:
`v055-sku-admin` lesson, disabled-gate must match submit validator).

**Fix:** the targeted change the repro identifies — e.g. make the input emit a
proper change event under `fill`, fix the onChange filter, or align the
disable predicate. Minimal, no unrelated form refactor.

**Un-skip:** `spoilage`.

### 4.4 B-final — CI + board

Confirm `.github/workflows/e2e.yml` runs the now-active specs. Update the v0.6
PROGRESS "⚠️ Partial" note to reflect actual coverage. Any cluster that can't be
fully fixed in v0.6.1 leaves its specs skipped **with an updated, evidenced
skip-comment** and a residual tracked task — surfaced explicitly, never a silent
partial or a false-green un-skip.

---

## 5. Board reconciliation (rides in this PR)

- `v06-fu-voucher-shared-helper` → ✅ (`e2b1184`, landed in #56).
- `v06-fu-spoilage-comment-fix` → ✅ (`e2b1184`, landed in #56).
- `v06-fu-actioncache-authcheck` → folded into Wave A tasks.
- `v06-fu-e2e-unskip` → folded into Wave B tasks.
- New v0.6.1 phase block with Wave A / Wave B tasks added via `/progress-update --new-task`.

---

## 6. Testing strategy

- **Wave A:** Vitest (convex-test). Per-action auth-before-cache regression tests +
  existing green suite. `npm run typecheck` + targeted `npx vitest run` on the
  touched modules. Full `npm test` before landing.
- **Wave B:** Playwright. B1 is manual/instrumented reproduction (evidence, not a
  passing test yet). B2+ measured by each un-skipped spec going green in CI.

---

## 7. Risks & open questions

- **R1 (Wave A):** Adding a required 4th param to `withActionCache` is a breaking
  signature change touching 7 call sites in one commit. Mitigation: mechanical,
  covered by typecheck + per-site tests; all sites change identically.
- **R2 (Wave B):** a cluster's documented cause may have shifted on current `main`
  (the PR-#52 runs predate v0.6.1), or a fix may be deeper than the id-plumbing /
  seed / form change anticipated (e.g. the Xendit simulate needs an endpoint change,
  not just the stored id). Mitigation: each cluster has a verify-on-main step first,
  and §4.4 allows a surfaced, evidenced residual rather than a false-green un-skip.
- **OQ1:** Should an ESLint rule also enforce "every `withActionCache` site passes
  a real manager authCheck"? With a *required* param the type system already
  forces *an* authCheck; a lint rule would additionally assert it's a
  `assertManagerSessionInAction` call. **Proposed:** defer — required param +
  ADR-046 is sufficient for v0.6.1; revisit if a non-conforming authCheck appears.
- **OQ2:** Does `assertManagerSessionInAction` belong in `verifyPin.ts` or a new
  `auth/actionAuth.ts`? **Proposed:** `verifyPin.ts` (already the action-side
  manager funnel) to keep the auth surface in one file.
- **OQ3 (staffreview):** Single resolver vs two? `verifyManagerPinOrThrow` resolves
  session+role via `getSession`+`_getStaffPinHash_internal`; the pre-cache gate
  uses `_resolveSessionRole_internal`. **Proposed:** keep two for v0.6.1 + enforce
  parity by test (§3.2); only unify if the parity test is awkward. Tracked, not
  blocking.

### 7a. Deferred to the plan (staffreview structural fill)

Spec-stage doc; the implementation plan MUST add: (1) exact file-change list with
paths per task; (2) commit-per-task boundaries (`feat:`/`test:`/`docs:` per the
repo convention); (3) **rollback** — Wave A is a single atomic signature change +
7 call sites, revertable as one commit; no data migration; (4) **deployment** —
`npx convex deploy` for the backend; **no schema change, no migration ordering,
no `SCHEMA.md` update** (pure code); CLAUDE.md rule #20 / file-locations + ADR-046
+ CHANGELOG are the only docs to touch; (5) per-task agent assignment
(`convex-expert` for Wave A, `general-purpose` for Wave B e2e).

---

## 8. Out of scope

- The empty lockout-test stub (`e2e/specs/auth.spec.ts:24`) — separate follow-up.
- Any new e2e coverage beyond the 6 existing specs.
- Mutation-layer idempotency (unchanged).
- The `recount`/`stock_in`/`adjustment` inventory mutations (future phase).
