# Staff Review: v2.0 Multi-outlet Foundation — Implementation Plan

**Date:** 2026-06-21
**Plan:** `docs/superpowers/plans/2026-06-21-v2.0-multi-outlet-foundation.md`
**Reviewers:** Staff Developer + Principal Developer (self-review, fresh-eyes pass)
**Plan Structure:** ✅ Validated (Goal, Global Constraints, File Structure, 13 TDD tasks, Self-Review w/ verify-first list, CHANGELOG, rollback in Tasks 11/12)

---

## 1. Summary

**Overall Assessment:** Approve (after the two harness fixes below, applied inline)

The plan is executable: real signatures, load-bearing migration ordering split across deployable tasks (Step 1 = T1–9, backfill = T11, enforce = T12), the lint fence as the scoping-completeness oracle (T8→T9), and all Gate-1 catches folded in (settlements exclusion test T11, three-writer sweep T7, `_allocateReceiptNumber_internal` T4). Two concrete code-truth corrections found by verifying against real test files; both fixed in place.

## 2. Critical Issues (Must Fix)

| # | Issue | Category | Location |
|---|-------|----------|----------|
| C1 | Test snippets use `convexTest(schema, modules)` + a non-existent `test.setup` import | Testing | every TDD test block |

### Issue C1: wrong convex-test harness call
Existing tests (`convex/transactions/__tests__/*.test.ts`) call **`convexTest(schema)`** — one arg, no `modules` glob, no `convex/test.setup.ts`. The plan's snippets import `{ modules } from "../../test.setup"` and pass it as a 2nd arg; they would throw at import on the first run. **Fix (applied):** `convexTest(schema)` everywhere; removed the bogus import.

## 3. Improvements (Recommended)

| # | Improvement | Impact | Effort |
|---|-------------|--------|--------|
| I1 | `receipt_footer` field name wrong | L | L |

### Improvement I1: settings field name
Plan's Task-5 test referenced `receipt_footer`; the real field is **`receipt_footer_text`** (`convex/settings/schema.ts:10`). **Fix (applied).**

## 4. Refinements (Optional)
- Task 9 is the largest task (call-site scoping across ~10 modules); the lint fence makes it self-verifying, but execution may split it per-module for reviewable commits — left to the executor.

## 5. Duplication Analysis
No new duplication. Plan reuses `no-cross-module-db-access` structure, `_getSettings_internal` defaults pattern, `verifyManagerPinOrThrow`, the existing receipt allocator, and the idempotency dual-call pattern. ✅

## 6. Phase / Wave Accuracy
Deploy phases correctly mapped to tasks; optional→backfill→enforce ordering honored; enforce (T12) gated on `assertZeroNullOutletIds`. ✅

## 7. Specialist Agent Recommendations
`convex-expert` (T2/T4/T5/T7/T11/T12 schema+migration), `frontend-integrator` (T10), `general-purpose` (T8 AST rule). All exist in the roster.

## 8. Git Workflow Assessment
Branch `feat/v2.0-multi-outlet-foundation`; one commit per task; typecheck/lint/vitest gate per task + full gate before merge; rollback documented (Steps 1–2 additive/idempotent, Step 3 one-way behind a verification query). ✅

## 9. Documentation Checkpoints
Task 13 covers SCHEMA.md, CLAUDE.md, ADR-051 status, CHANGELOG. ✅

## 10. Testing Plan Assessment
**Verdict:** Adequate (after C1 fix). Covers helper unit tests, cross-outlet denial, `SESSION_NO_OUTLET`/`DEVICE_HAS_NO_OUTLET`, two-outlet receipt sequences, the settlements-exclusion migration test, managerTakeover outlet stamp, lint-fence fixtures + scratch-injection liveness, e2e login/roster. Money path (receipt NNNN) has known-value assertions.

## 4.9 Evidence-Before-Mitigation Gate
N/A — this is a feature/migration plan, not a flake/race fix. No mitigation-vs-fix ambiguity.

## 11. Edge Cases to Address
- [x] Pre-backfill old active session (I4 tolerant window) — T3 test
- [x] managerTakeover/seed session outlet stamp — T7
- [x] settlements excluded from backfill — T11
- [x] unbound-device login (`DEVICE_HAS_NO_OUTLET`) — T7/T10

## 12. Approval Conditions
**To approve:** C1 (applied). **Recommended:** I1 (applied). → **Approved.**

---

*Generated by /staffreview*
