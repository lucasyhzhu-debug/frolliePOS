# Staff Review: Two-Level Booth State — Implementation Plan

**Date:** 2026-06-25
**Plan:** `docs/superpowers/plans/2026-06-25-outlet-shift-two-level-state.md`
**Reviewers:** Staff Developer (Implementation) + Principal Developer (Architecture)
**Plan Structure:** ✅ Validated (Task List + Execution Strategy + per-task TDD + migration/rollback present)

## 1. Summary

**Overall: Approve after fixes (applied inline).** The plan is well-decomposed (13 tasks + T5B), TDD throughout, with a clear wave map and shared-file serialization rule for `convex/shifts/shifts.ts`. The plan staffreview's job — verifying flagged assumptions against real code — surfaced three concrete name/invocation errors, all fixed inline.

## 2. Critical Issues (fixed inline)

### C1 — Wrong invocation form + module path for the manager-PIN actions (Task 4)
Task 4's `managerSkipOpen` called `ctx.runAction(internal.auth.verifyPin.verifyManagerPinOrThrow, ...)` and `ctx.runQuery(internal.auth.sessions._resolveSession_internal, ...)`. Verified against code:
- `verifyManagerPinOrThrow`/`verifyPinOrThrow` are **plain async functions** in `convex/auth/verifyPin.ts` (`:98`/`:24`) — called inline (`await verifyPinOrThrow(ctx, {...})`), NOT via `ctx.runAction`. The working `managerTakeover` in `actions.ts` imports `verifyPinOrThrow` and calls it inline after a `role === "manager"` check.
- `_resolveSession_internal` is in `convex/auth/internal.ts` (`:347`) → `internal.auth.internal._resolveSession_internal`, not `internal.auth.sessions.*`.
**Fix:** Task 4 snippet rewritten to mirror `managerTakeover` exactly (inline `verifyPinOrThrow`, correct module path). Task 8 already says "model on managerTakeover" — left as-is with the same note.

## 3. Improvements (addressed inline)

- **`_sendSignoffSummary.eventId` widening** — confirmed `v.id("pos_shift_events")` (`actions.ts:109`); the per-shift summary now keys off a `pos_shifts` id, so Task 5 widens it to `v.union(v.id("pos_shift_events"), v.id("pos_shifts"))`. Recorded in resolved-confirmations.
- **`/shift/handover` removal** — confirmed route (`router.tsx:122`) + lazy import (`:52`); Task 12 removes both and adds session-FULL `/shift/begin` (Task 12 Step 3B).
- **Spec C1 propagation** — `startShift` (T5B) added as a full TDD task; FE login target routes the open/no-holder case to `/shift/begin` (Task 11) which calls it.
- **Spec C2 propagation** — Task 10 migration now inlines the status derivation (no `deriveBoothState` import), so the post-deploy backfill survives Task 13's deletion.

## 4. Verified-against-code (no issue)

- `internal.auth.internal._getDeviceOutletId_internal` / `_listStaffNames_internal` / `_endShiftSession_internal` — all present in `auth/internal.ts`. ✓
- `_buildSignoffSummary_internal`, `_shiftStartAnchor_internal` — present in `shifts/internal.ts`. ✓
- `pos_shifts.by_outlet_active` leads with `outlet_id` — outlet-fence compliant. ✓

## 5. Execution / Waves

Wave map is sound. The load-bearing constraint is correctly captured: **`convex/shifts/shifts.ts` is written by T4/T5/T5B/T6/T7/T9 → serialize** (one writer; or per-task worktrees merged in order with one codegen per wave). Critical path T1→T3→T4→T9→T11→T12→T13 is realistic; Wave-3 serialization is the long pole.

## 6. Testing Plan Assessment

**Verdict: Adequate.** Every backend task is TDD (failing test → impl → pass). Migration has a backfill test (locked→open+holder; signoff→closed). FE tasks have render/interaction tests for the gate (resume/block/new/override). Full gate (`typecheck && vitest`) at Wave 6. Headless-impossible items (prod backfill run, live booth UAT, bilingual smoke, enforce-flip) correctly flagged "pending," not claimed passed.

## 7. Rollback / Deployment

Additive→enforce migration, prod-safe. Deploy atomicity (public-function renames are deploy-skew-fatal) flagged in spec §9 and respected by the single Vercel prod build. The backfill self-heals the live stuck booth.

## 8. Approval

**Approved for execution** with C1 + the improvements applied (done). One residual executor note: copy the exact manager role-resolve query name verbatim from `managerTakeover` (`actions.ts`).

---
*Generated by /staffreview*
