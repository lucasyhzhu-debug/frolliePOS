# Staff Review: v0.5.4 — BT thermal printing (PLAN gate)

**Date:** 2026-06-02
**Plan:** `docs/superpowers/plans/2026-06-02-bluetooth-thermal-printing.md`
**Reviewers:** Staff Developer (Implementation) + Principal Developer (Architecture)
**Focus:** verify the plan's flagged assumptions against real code (the cheap, high-value catches).

---

## 1. Summary

**Overall: Approve (fixes applied inline).** The plan is well-decomposed (TDD on the three pure/testable units, manual on the unmockable BLE layer, frequent commits). Verification against the codebase found **2 wrong references** and **1 missing primitive**; all three are fixed in the plan. Library-API uncertainty (the ESC/POS encoder) is correctly isolated behind a robust golden test.

---

## 2. Verification results

| # | Plan assumption | Verified against | Result | Action |
|---|-----------------|------------------|--------|--------|
| A | `shareReceipt` at `api.transactions.shareReceipt` | `src/routes/history/$txnId.tsx:48` | ❌ **Wrong** — it's `api.transactions.public.shareReceipt` | **Fixed** Task 7 |
| B | Use `useIdempotency` for the `shareReceipt` key | `history/$txnId.tsx:55-60` comment + `drafts.tsx` convention | ❌ **Against convention** — one-shot user actions use `crypto.randomUUID()`; `useIdempotency` is for retried/replayed mutations | **Fixed** Task 7 (drop `useIdempotency` import) |
| C | `src/components/ui/sheet.tsx` (shadcn Sheet) | `ls src/components/ui` → only `dialog.tsx`, `popover.tsx`; `PinSheet.tsx` wraps `Dialog` | ❌ **Missing** — convention is "Sheet" components wrap `Dialog` | **Fixed** Task 6 (rebuilt on `Dialog`, mirrors `PinSheet`) |
| D | `_getPaidTxnWithLinesForReceipt_internal` returns `{ txn: { created_at } }` | `convex/transactions/internal.ts:405-417` (`txn = ctx.db.get(...)`, full doc) | ✅ **Holds** — `txn.created_at` present | none |
| E | `@point-of-sale/esc-pos-encoder` chainable API | npm (two divergent successor packages exist) | ⚠️ **Ambiguous** — classic vs `receipt-printer-encoder` differ | Note strengthened; pinned to classic pkg; golden test asserts decoded-text substrings (robust to method-name drift) |
| F | `buildReceiptUrl(token)` client helper | `src/lib/format.ts:62` | ✅ exists | none |
| G | `getReceiptForPrint` / `getTransactionDetail` role+today scope | `transactions/public.ts:486-526` | ✅ pattern mirrored faithfully | none |

---

## 3. Critical Issues

All three Criticals were found by verification and **fixed inline** (rows A, B, C above). No open Criticals.

## 4. Improvements (applied)

- Task 7 now matches the established one-shot-action idempotency convention (`crypto.randomUUID()`), and documents *why* reprints still yield a stable QR (shareReceipt is idempotent by txn, not by key).
- Encoder assumption note now names both successor packages and the exact API points to confirm (`.size` vs `.width/.height`, `.line` vs `.text().newline()`), so the implementer doesn't guess.

## 5. Testing assessment

**Verdict: Adequate.**
- BE: `getReceiptForPrint` — paid happy-path, invalid-session→null, staff-out-of-today→null, manager-any (4 convex-test cases, real seed shape mirrored from `cancelAwaitingPayment.test.ts`).
- Encoder: golden tests assert ESC @ prefix + decoded substrings (`R-2026-0042`, `LUNAS`, `Rp 325.000`, no emoji) with a **fixed `paid_at`** → deterministic.
- `chunkBytes`: 4 boundary cases (empty / < / exact / > MTU) — covers the truncation failure class.
- BLE hook + UI: manual on-device checklist (correct — Web Bluetooth is unmockable).

## 6. Edge cases (covered in plan)

- `unsupported` browser → hidden print UI.
- `printData === null` (out-of-scope/not-paid) → button disabled, `onPrint` guards.
- disconnect mid-print → hook sets `error`, toast, no crash.
- reprint → same token (stable QR).

## 7. Rollback / deployment

Additive; no schema change. Deploy Convex before Vercel FE. Revert commits + drop deps to roll back. ✅

## 8. Approval

**Approved for execution** after the inline fixes (committed alongside this review). Verify-first list for the executor:
1. `@point-of-sale/esc-pos-encoder` method names (`.size`/`.width`/`.line`) post-install.
2. Native ESC/POS QR vs raster fallback on the CEVA FW.
3. `getDevices()` availability on the booth's Chrome.

---

*Generated by /staffreview (plan gate)*
