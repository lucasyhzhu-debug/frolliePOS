# Staff Review (Plan Gate): v1.0.2 Sales-Ticker Toggle

**Date:** 2026-06-19
**Plan:** `docs/superpowers/plans/2026-06-19-v1.0.2-sales-ticker-toggle.md`
**Reviewers:** Staff Developer + Principal Developer
**Plan Structure:** ✅ Validated (header, Global Constraints, 3 tasks w/ TDD steps, Success Criteria, Deployment/Rollback, Self-Review all present)

## 1. Summary

**Overall Assessment:** Approve.

The plan is concrete and TDD-structured, every code step shows real code, and — critically — its
load-bearing assumptions were verified against the actual codebase (below). It also self-corrected the
spec's one inaccuracy (the `setupQueryMock` over-specification). No Critical or Improvement issues; two
Refinements only.

## 2. Assumption Verification (the point of this gate)

| Assumption in plan | Verified? | Evidence |
|---|---|---|
| `setupQueryMock` is args-shape-dispatched (no change needed for 2nd `getSettings`) | ✅ | `telegram-chats.test.tsx:117-124` — branches on `"includeArchived" in args` |
| `setupMutationMock` is call-order-indexed via `order` array + modulo | ✅ | `telegram-chats.test.tsx:134-144` |
| Stub decls are module-level `let`, reset per `beforeEach` | ✅ | `:82-86`, resets at `:164-168`, `:211-215`, founders block |
| `ToggleResult` type already exists to reuse | ✅ | `settings/public.ts:19` |
| `setFoundersSummaryEnabled` shape to clone (idempotency+authCheck+audit+insert default) | ✅ | `settings/public.ts:21-74` |
| `txn_ticker_enabled` already in schema (no migration) | ✅ | `settings/schema.ts:23` |
| Existing settings tests' seed shape + `MANAGER_ONLY` reject + audit-count pattern | ✅ | `settings.test.ts:12-59,82-109` |
| `useState` + `errorMessage` already imported on the FE page | ✅ | `telegram-chats.tsx:12,32` |
| RUNBOOK kill-switch text to replace | ✅ | `docs/RUNBOOK.md:248` |
| SCHEMA field note to update | ✅ | `docs/SCHEMA.md:562` (plan says ~563 — off-by-one, harmless) |
| CLAUDE rule #22 manager-session sentence to append | ✅ | `CLAUDE.md:69` |
| Ticker disabled → silent skip (acceptance #4 mechanism) | ✅ | `txnTicker.ts:39` |

Every assumption the plan relies on is real. This is the strongest possible outcome for the plan gate.

## 3. Critical Issues

None.

## 4. Improvements

None.

## 5. Refinements (Optional)

- **R1 — Audit-verb list location.** Task 3 Step 1 says "add beside `settings.founders_summary_toggled`."
  If that verb isn't yet enumerated in SCHEMA.md's list, just add `settings.txn_ticker_toggled` to
  whatever audit-verb section exists; the instruction is robust either way. No blocker.
- **R2 — Multi-card modulo caveat (pre-existing).** `setupMutationMock`'s modulo only aligns for the
  single-chat-card render the existing tests use (founders + ticker + 3 card mutations = 5 = `order.length`).
  This limitation predates this plan; the new ticker tests use `[]` or single-card fixtures, so they're
  safe. Worth a one-line awareness note for the executor, not a change.

## 6. Testing Plan Assessment

**Verdict:** Adequate.

Backend: flip both directions, insert-default-founders (anti-clobber), staff-rejection (`MANAGER_ONLY`),
idempotent replay with audit-count + parsed-metadata assertion. Frontend: checked/unchecked render +
both-direction flip calls, plus an explicit re-run of the FULL suite to prove the mock re-index didn't
regress founders/chat-card tests (Task 2 Step 4 + Step 7). Regression risk is directly tested. ✅

## 7. Git Workflow

Three atomic commits at natural boundaries (backend / frontend / docs), each with a typecheck or test
gate before commit. Squash-PR via the pipeline worktree. ✅

## 8. Deployment & Rollback

Plan states it: additive mutation, no skew hazard, atomic build deploy, revert-to-rollback, dashboard
break-glass retained, per-deployment isolation. ✅

## 9. Approval Conditions

None outstanding. **Approved for execution.** Refinements R1/R2 are executor-discretion awareness notes.

---

*Generated by /staffreview (plan gate)*
